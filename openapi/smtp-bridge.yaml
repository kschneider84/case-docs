---
openapi: 3.1.0
info:
  title: SMTP Bridge API
  version: 1.0.0
  description: Simple bridge for sending emails via configured SMTP servers.
tags:
- name: default
  description: Default operations for SMTP Bridge API
paths:
  "/":
    get:
      tags:
      - default
      summary: Health check
      operationId: getHealth
      responses:
        '200':
          description: Service is healthy
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/HealthStatus"
              examples:
                ok:
                  value:
                    status: ok
  "/send-email":
    post:
      tags:
      - default
      summary: Send an email
      operationId: sendEmail
      security:
      - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/SendEmailRequest"
            examples:
              basic:
                value:
                  to:
                  - user@example.com
                  subject: Hello
                  content: "<p>Hi there</p>"
                  highPrio: false
              withHeaderOverrides:
                value:
                  to:
                  - user@example.com
                  subject: Important
                  content: Text body
                  highPrio: true
                  headerOverrides:
                    from: '"System Notifications" <noreply@example.com>'
                    sender: system@example.com
                    replyTo:
                    - support@example.com
                    noReplyTo: false
      responses:
        '200':
          description: SMTP sending completed successfully. The handler waits for the SMTP send operation to finish before responding.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/SuccessMessage"
              examples:
                sent:
                  value:
                    message: email sent
        '400':
          description: Missing or invalid API key, missing request payload, invalid JSON or field types, or an empty recipient list.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/Error"
              examples:
                missingApiKey:
                  value:
                    error: A valid API key missing
                missingTo:
                  value:
                    error: missing 'to' field
                missingPayload:
                  value:
                    error: payload missing
        '500':
          description: Failed to send after retries
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/Error"
              examples:
                sendFailed:
                  value:
                    error: failed to send email
components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: Api-Key
      description: A configured SMTP bridge API key.
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
      - status
      additionalProperties: false
    SuccessMessage:
      type: object
      properties:
        message:
          type: string
      required:
      - message
      additionalProperties: false
    Error:
      type: object
      properties:
        error:
          type: string
      required:
      - error
      additionalProperties: false
    HeaderOverrides:
      type: object
      properties:
        from:
          type: string
          description: Overrides the configured From header when nonempty.
        sender:
          type: string
          description: Overrides the configured Sender header when nonempty.
        replyTo:
          type: array
          description: Overrides the configured Reply-To addresses when nonempty and noReplyTo is false.
          items:
            type: string
            format: email
        noReplyTo:
          type: boolean
          default: false
          description: Clears the Reply-To addresses when true, taking precedence over replyTo and the configured defaults.
      additionalProperties: true
    SendEmailRequest:
      type: object
      properties:
        to:
          type: array
          items:
            type: string
            format: email
          minItems: 1
        subject:
          type: string
        content:
          type: string
          description: HTML email body.
        highPrio:
          type: boolean
          default: false
          description: Uses the high-priority SMTP client pool when true; otherwise uses the low-priority pool.
        headerOverrides:
          description: Optional email header overrides. When omitted or null, the configured defaults are used.
          anyOf:
          - "$ref": "#/components/schemas/HeaderOverrides"
          - type: 'null'
      required:
      - to
      additionalProperties: true
